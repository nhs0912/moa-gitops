apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sajin-moa-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging

spec:
  ingressClassName: traefik

  tls:
    - hosts:
        - sajin-moa-dev.duckdns.org
      secretName: sajin-moa-dev-tls

  rules:
    - host: sajin-moa-dev.duckdns.org
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sajin-moa-backend
                port:
                  number: 8080

          - path: /
            pathType: Prefix
            backend:
              service:
                name: sajin-moa-frontend
                port:
                  number: 80